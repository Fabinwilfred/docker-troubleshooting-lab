services:
  writer:
    image: alpine:3.20
    container_name: lab-volume-writer
    command: ["sh", "-c", "date > /data/created-at; tail -f /dev/null"]
  reader:
    image: alpine:3.20
    container_name: lab-volume-reader
    command: ["sh", "-c", "tail -f /dev/null"]
volumes:
  lab_data: {}
